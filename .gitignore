# The raw Microsoft Forms export is the project's source of truth (see PURPOSE.md),
# but it is held OUT of git: this repository is public, and the export contains
# individual responses plus the Email, Name and Q39 "Your full name" fields, which
# respondents were told would be used for internal follow-up only.
#
# Keep the full export locally / in OneDrive. What is versioned instead is the
# anonymized copy re-exported below, with those three columns removed.
*.xlsx
!AI Usage Assessment — NICE UX Team(1-37) — anonymized.xlsx

.DS_Store
